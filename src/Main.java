import javax.swing.*;
import java.awt.*;
public class Main {
    public static void main (String [] args) {
  // SimpleWindow myWindow = new SimpleWindow(new StudentList());
     Controller controller = new Controller();
    }
}